import styled from 'styled-components'


export const StyledQueryInput = styled.div`
    {
        display: flex;
        flex-direction: column;
        justify-content: center;
        width: min(98%, 20rem);
        margin-inline: auto;
        padding-top: 0.5rem;
        
        button {
            display: block;
            width: min-content;
            margin-left: auto;
    }`

export default StyledQueryInput